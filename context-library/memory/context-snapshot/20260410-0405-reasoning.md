# Reasoning Log — 20260410-0405

## Direction Changes Mid-Session

**Desktop app scope**
- **Original direction:** Desktop app primarily for transcript import UX
- **What changed:** Realised desktop app eliminates the need for Cloudflare Worker entirely
- **Why:** Native apps can hold API keys securely and call Anthropic directly — no proxy needed
- **New direction:** Desktop app as the primary interface for real users; web app stays only for public demo

**Thought partner as a feature vs. core concept**
- **Original direction:** "Thought partner" raised as an addition to the existing app
- **What changed:** Recognised this reframes the entire product — not a viewer with annotation bolted on, but a negotiation layer between two reads
- **Why:** The most valuable signal is where Claude's synthesis and the user's judgment diverge. The current model treats Claude's output as the answer. That's wrong.
- **New direction:** Thought partner is the core interaction model. The annotation schema must be designed before the desktop app is built.

## Assumptions Still Unvalidated

- **Tauri is the right desktop framework** — assumed based on small installer size and Rust backend. Not evaluated against Electron in detail. If wrong: Electron is more mature but heavier (~200MB vs ~10MB).
- **Users will engage with the annotation layer** — the thought partner model only works if users actually review and react to synthesis. If the friction is too high, they'll skip it and the negotiated view never develops.
- **Synthesis as the quality gate for global memory is sufficient** — assumes `/_project-synthesis` produces signal-rich enough output to be worth promoting. If synthesis quality is low, the global graph fills with noise.

## Exploratory Threads (No Conclusion)

- **Where does the dialogue happen — UI or Ask tab?** Discussed both. Conclusion: UI handles simple accept/reject/hypothesise; Ask tab handles nuanced disagreements. Both feed the same annotation layer. Not fully designed.
- **How projects-registry.md gets maintained** — discussed that it needs a skill to add/update entries. Which skill? New `/_new-project` variant? Not resolved.
- **Timeline for desktop app** — user acknowledged the app is getting complex. No timeline set. Annotation schema design is the prerequisite before any desktop work starts.
